# TEMPLATE ONLY — do not commit real values.
# Either fill this in, save as k8s/secret.yaml (gitignored) and `kubectl apply -f`,
# or create it imperatively:
#
#   kubectl -n rom-catalog create secret generic rom-catalog-api-secret \
#     --from-literal=JWT_SECRET="$(openssl rand -base64 48)" \
#     --from-literal=DB_URL="jdbc:postgresql://postgres.db.svc.cluster.local:5432/romcatalog" \
#     --from-literal=DB_USER="romcatalog" \
#     --from-literal=DB_PASSWORD="..." \
#     --from-literal=MINIO_ACCESS_KEY="..." \
#     --from-literal=MINIO_SECRET_KEY="..."
apiVersion: v1
kind: Secret
metadata:
  name: rom-catalog-api-secret
  namespace: rom-catalog
type: Opaque
stringData:
  JWT_SECRET: "CHANGE-ME-long-random-value"
  DB_URL: "jdbc:postgresql://postgres.db.svc.cluster.local:5432/romcatalog"
  DB_USER: "romcatalog"
  DB_PASSWORD: "CHANGE-ME"
  MINIO_ACCESS_KEY: "CHANGE-ME"
  MINIO_SECRET_KEY: "CHANGE-ME"
